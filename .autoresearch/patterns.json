{
  "hardcoded_responses": [
    "[main.py] Hardcoded metrics dict with keys: {'recall', 'accuracy', 'precision', 'f1'}"
  ],
  "sleep_simulation": [
    "[main.py] *.sleep() call (duration=5) \u2014 likely simulation",
    "[main.py] Loop body contains only print/sleep \u2014 likely fake training",
    "[main.py] Loop body is only print/sleep \u2014 fake training"
  ],
  "import_failures": [
    "No real ML/AI libraries imported (only stdlib)",
    "Only standard library imports \u2014 no real ML/AI libraries",
    "LLM review: 2/10 \u2014 The code is truncated mid-import (`import` with no module name), indicating it is incomplete and cannot run, making it impossible to demonstrate the claimed concept."
  ],
  "no_output_variance": [
    "No entry point for variance check",
    "Identical output across 2 runs (14 chars)"
  ],
  "poor_loss_improvement": [
    "Training loop detected but no loss/metric tracking",
    "LLM review: 4/10 \u2014 Training and evaluating on the same data produces artificially inflated scores (no train/test split), `random_state=None` makes results non-reproducible, and with only 10 estimators",
    "ML project with no training detected"
  ]
}